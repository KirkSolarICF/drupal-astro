---
import type { HTMLAttributes } from "astro/types";
interface Props extends HTMLAttributes<"a"> {
  href: string;
  varient?: "primary" | "secondary";
    
}
const { href, varient = "primary", ...rest }: Props = Astro.props;
---
<a href={href} {...rest} >
    <slot />
</a>